---
interface Props { class?: string; }
const { class: className = "" } = Astro.props;
---
<span class={`inline-block rounded-md border border-[var(--color-line)] px-2 py-0.5 text-[11px] uppercase tracking-wider text-[var(--color-ink-soft)] ${className}`}>
  <slot />
</span>
